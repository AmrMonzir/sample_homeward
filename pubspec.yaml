name: sample_homeward
description: Sample Flutter app with login and blog information for Homeward.co 

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^4.1.0
  email_validator: ^2.0.1
  cupertino_icons: ^1.0.2
  get: ^4.1.1
  cached_network_image: 3.0.0-nullsafety
  shimmer: 2.0.0-nullsafety.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
